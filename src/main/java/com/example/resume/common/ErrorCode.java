package com.example.resume.common;

/**
 * 错误码枚举 / Error code enum
 */
public enum ErrorCode {
    VALIDATION_ERROR,
    USERNAME_ALREADY_EXISTS,
    USER_NOT_FOUND,
    PROFILE_NOT_FOUND,
    SERVICE_NOT_FOUND,
    UNAUTHORIZED,
    FORBIDDEN,
    FILE_UPLOAD_FAILED,
    INTERNAL_ERROR,
    PORTFOLIO_NOT_FOUND
}
